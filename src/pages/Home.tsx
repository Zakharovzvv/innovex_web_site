import React, {FC} from 'react';
import MainLayout from "../layouts/MainLayout";
import {Button} from "@mui/material";

const Home: FC = () => {
    return (
        <MainLayout>
            Home

        </MainLayout>
    );
};

export default Home;