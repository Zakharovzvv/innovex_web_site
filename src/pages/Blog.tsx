import React, {FC} from 'react';
import MainLayout from "../layouts/MainLayout";

const Blog :FC= () => {
    return (
        <MainLayout>
           Blog
        </MainLayout>
    );
};

export default Blog;