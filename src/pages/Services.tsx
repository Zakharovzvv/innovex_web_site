import React, {FC} from 'react';
import MainLayout from "../layouts/MainLayout";

const Services :FC= () => {
    return (
        <MainLayout>
            Services
        </MainLayout>
    );
};

export default Services;