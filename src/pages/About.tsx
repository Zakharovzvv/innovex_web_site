import React, {FC} from 'react';
import MainLayout from "../layouts/MainLayout";

const About :FC= () => {
    return (
        <MainLayout>
            About
        </MainLayout>
    );
};

export default About;