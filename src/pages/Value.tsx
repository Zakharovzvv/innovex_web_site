import React, {FC} from 'react';
import MainLayout from "../layouts/MainLayout";

const Value :FC= () => {
    return (
        <MainLayout>
            Value
        </MainLayout>
    );
};

export default Value;